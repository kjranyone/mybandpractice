import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.donoy.mybandpractice",
  appName: "My Band Practice",
  webDir: "dist",
};

export default config;
